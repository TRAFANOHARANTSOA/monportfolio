<?php session_start();?>
<?php include 'header.php'?>
<div id="wrapper">
  <div id="bg"></div>
  <!--<div id="overlay"></div>-->
</div>

<?php


                      /*subject.addEventListener("input", function (event) {
                        // Chaque fois que l'utilisateur saisit quelque chose
                        // on vérifie la validité du champ e-mail.
                        if (subject.validity.valid) {
                          // S'il y a un message d'erreur affiché et que le champ
                          // est valide, on retire l'erreur
                          errorsubject.innerHTML = ""; // On réinitialise le contenu
                          errorsubject.className = "errorsubject"; // On réinitialise l'état visuel du message
                        }
                        }, false);

                        form.addEventListener("submit", function (event) {

                        if (!subject.validity.valid) {

                          // S'il est invalide, on affiche un message d'erreur personnalisé
                          errorsubjec.innerHTML = "Hey, un nom commence toujours par un majuscule voyons!";
                          errorsubjec.className = "errorsubject active";
                          // Et on empêche l'envoi des données du formulaire
                          event.preventDefault();
                        }
                      },false);


                        name.addEventListener("input", function (event) {
                          // Chaque fois que l'utilisateur saisit quelque chose
                          // on vérifie la validité du champ e-mail.
                          if (name.validity.valid) {
                            // S'il y a un message d'erreur affiché et que le champ
                            // est valide, on retire l'erreur
                            errorname.innerHTML = ""; // On réinitialise le contenu
                            errorname.className = "errorname"; // On réinitialise l'état visuel du message
                          }
                          }, false);

                          form.addEventListener("submit", function (event) {

                          if (!name.validity.valid) {

                            // S'il est invalide, on affiche un message d'erreur personnalisé
                            errorname.innerHTML = "Hey, un nom commence toujours par un majuscule voyons!";
                            errorname.className = "errorname active";
                            // Et on empêche l'envoi des données du formulaire
                            event.preventDefault();
                          }
                        },false); */
 ?>

<?php include 'footer.php'?>
